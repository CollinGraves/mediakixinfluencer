<%= simple_form_for(@platform_campaign) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :contract_num %>
    <%= f.association :brand %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
